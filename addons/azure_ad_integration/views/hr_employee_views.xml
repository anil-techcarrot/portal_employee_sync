<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Employee Form with License Management -->
    <record id="view_employee_form_azure_license" model="ir.ui.view">
        <field name="name">hr.employee.form.azure.license</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">

            <!-- Azure AD Info Tab -->
            <xpath expr="//notebook" position="inside">
                <page string="Azure AD Info" name="azure_ad_info">

                    <!-- IMPORTANT: Load all fields first in a hidden group -->
                    <group invisible="1">
                        <field name="azure_license_assigned"/>
                        <field name="azure_user_id"/>
                    </group>

                    <group>
                        <group string="Azure Account Details">
                            <field name="azure_email" readonly="1"/>
                            <field name="azure_user_id" readonly="1" force_save="1"/>
                        </group>
                        <group string="License Information">
                            <field name="azure_license_assigned" readonly="1" force_save="1"/>
                            <field name="azure_license_name" readonly="1"
                                   invisible="not azure_license_assigned"/>
                        </group>
                    </group>

                    <!-- License Management -->
                    <group string="License Management">
                        <button name="action_unassign_license"
                                type="object"
                                string="Unassign License"
                                class="btn-warning"
                                icon="fa-minus-circle"
                                confirm="Are you sure you want to remove the license from this employee?"
                                invisible="not azure_license_assigned"/>
                    </group>

                    <!-- Quick Actions -->
                    <group string="Quick Actions">
                        <button name="action_view_azure_user"
                                type="object"
                                string="View in Azure Portal"
                                class="btn-primary"
                                icon="fa-external-link"
                                invisible="not azure_user_id"/>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

    <!-- Employee Tree View -->
    <record id="view_employee_tree_azure" model="ir.ui.view">
        <field name="name">hr.employee.tree.azure</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='work_email']" position="after">
                <field name="azure_email" optional="show"/>
                <field name="azure_license_assigned" optional="show"/>
                <field name="azure_license_name" optional="hide"/>
            </xpath>
        </field>
    </record>
</odoo>